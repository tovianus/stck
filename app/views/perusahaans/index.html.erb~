<% provide(:title, 'Daftar perusahaan') %>
<div class="row-fluid">
  <%= render "shared/page_title" %>
  <div id="pgnav" class="row-fluid span12 pull-left style="border:2px solid">
      <%= link_to('Tambah Perusahaan', new_perusahaan_path, :class => "btn btn-large" )  %>  
  </div>
  <div  class="row-fluid">
    <div class="span12">
        <%= will_paginate @perusahaans %>
        <table class="table">
        <thead>
          <tr>
            <th>Nama penanggung jawab</th>
            <th>Nama badan</th>
            <th>Alamat badan</th>
            <th>Telepon</th>
            <th>Nama kontak</th>
            <th>Kode perusahaan</th>
            <th>Total permohonan</th>
            <th colspan="3">&nbsp</th>
          </tr>
        </thead>
        <tbody>
        <% @perusahaans.each do |perusahaan| %>
          <tr>
            <td><%= perusahaan.nama_penanggung %></td>
            <td><%= perusahaan.nama_badan %></td>
            <td><%= perusahaan.alamat_badan %></td>
            <td><%= perusahaan.telepon %></td>
            <td><%= perusahaan.nama_kontak %></td>
            <td><%= perusahaan.kdperusahaan %></td>
            <td>
              <% if perusahaan.ttl_request>0 %>
                <span class="label label-success"><%= perusahaan.ttl_request %></span>
              <% else %>
                <span class="label label-warning"><%= perusahaan.ttl_request %></span>
              <% end %>
            </td>
            <td>
              <%= link_to 'Tampilkan', perusahaan,:class=>"btn btn-small" %>
              <%= link_to 'Rubah', edit_perusahaan_path(perusahaan),:class=>"btn btn-small" %>
              <% if perusahaan.ttl_request==0 %>
                <%= link_to 'Hapus', perusahaan, method: :delete, data: { confirm: 'Hapus data?' },:class=>"btn btn-small" %></td>
              <%end%>
          </tr>
        <% end %>
        </tbody>
      </table>
      <%= will_paginate @perusahaans %>
      </div>
    </div>
  </div>
</div>

